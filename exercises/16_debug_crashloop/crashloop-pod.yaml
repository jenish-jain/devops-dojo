# This pod is in CrashLoopBackOff! Can you fix it?
# Hint: Look at the command - does it run successfully?
apiVersion: v1
kind: Pod
metadata:
  name: crashloop-app
spec:
  containers:
  - name: app
    image: busybox:1.36
    command: ["sh", "-c", "echo 'Starting app...' && exit 1"]
    # The command above exits with error code 1, causing the pod to crash
    # Fix: Change to a command that stays running, like:
    # command: ["sh", "-c", "echo 'App running' && sleep 3600"]
